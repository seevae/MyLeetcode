package arrAndString.string;

public interface StringOperator {
    /**
     * Replace Spaces in the string with %20
     * @param srcStr
     * @return
     */
    String replaceSpace(String srcStr);
}
